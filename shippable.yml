language: python

python:
   - 2.7
   

before_install: true
#sudo apt-get install python-dev

install:
  - pip install -r requirements.txt

#script: python app.py

#commit_container: vidyar/sample_img_app
images:
  image:
    build:
      type: docker
      name: vidyar/sample_img_app:2.1
     # tag: latest
      code_path: /app.py
      
 
#build_image: shippable/minv2
#build_image:
 # name: shippable/minv2
  #net: host
  #privileged: true
  #run_command: 'ls -al'
#cache: true

#build_image:
 #  name:  vidyar/phpmongo:2.1
  # net: host
   #privileged: false
