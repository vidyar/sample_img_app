language: python

python: 2.7


install:
  - pip install -r requirements.txt

#script: python app.py
#build_image: vidyar/sample_img_app
build_image:
  name: vidyar/sample_img_app
  net: host
  privileged: true
  run_command: 'ls -al'
