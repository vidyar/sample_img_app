language: python

python: 2.7

before_install: sudo apt-get install python-dev

install:
  - pip install -r requirements.txt

#script: python app.py

#build_image:  vidyar/sample_img_app:latest

#build_image: shippable/minv2
#build_image:
 # name: shippable/minv2
  #net: host
  #privileged: true
  #run_command: 'ls -al'
#cache: true

build_image:
   name:  vidyar/phpmongo
   net: host
   privileged: true
